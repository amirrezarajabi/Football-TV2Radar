from utils.pitch import Pitch
from utils.solver import Solver
from utils.model import Model

class Point:
    def __init__(self, x, y, c="black") -> None:
        self.x = x
        self.y = y
        self.c = c

